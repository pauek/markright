// Markright Abstract Data Types

Markright = List(Paragraph | Command(Markright))
Paragraph = List(String | LineBreak | Command(Paragraph))

Command(T) = {
  name: String
  args: List(String)
  content: T
}
